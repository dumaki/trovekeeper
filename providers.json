{
  "_comment": "Single source of truth for provider secrets. Read by the server (config.ts), the setup script, and the doctor diagnostic. Add a provider here to make it appear in setup + doctor. 'secret: true' means the value is NEVER printed, logged, or echoed anywhere.",
  "providers": [
    {
      "id": "steam",
      "label": "Steam",
      "env": [
        {
          "key": "STEAM_API_KEY",
          "required": true,
          "secret": true,
          "hint": "32-char key from https://steamcommunity.com/dev/apikey"
        },
        {
          "key": "STEAM_ID",
          "required": true,
          "secret": false,
          "hint": "17-digit SteamID64 — look it up at https://steamid.io"
        }
      ]
    },
    {
      "id": "gog",
      "label": "GOG",
      "env": [
        {
          "key": "GOG_REFRESH_TOKEN",
          "required": true,
          "secret": true,
          "hint": "run `npm run gog-login` to obtain this — don't enter it by hand"
        }
      ]
    },
    {
      "id": "epic",
      "label": "Epic Games",
      "env": [
        {
          "key": "EPIC_REFRESH_TOKEN",
          "required": true,
          "secret": true,
          "hint": "run `npm run epic-login` to obtain this — don't enter it by hand"
        }
      ]
    },
    {
      "id": "psn",
      "label": "PlayStation",
      "env": [
        {
          "key": "PSN_NPSSO",
          "required": true,
          "secret": true,
          "hint": "run `npm run psn-login` — or paste the npsso from https://ca.account.sony.com/api/v1/ssocookie"
        }
      ]
    },
    {
      "id": "xbox",
      "label": "Xbox",
      "env": [
        {
          "key": "XBOX_REFRESH_TOKEN",
          "required": true,
          "secret": true,
          "hint": "run `npm run xbox-login` to obtain this — don't enter it by hand"
        }
      ]
    },
    {
      "id": "igdb",
      "label": "IGDB (time-to-beat, optional)",
      "env": [
        {
          "key": "IGDB_CLIENT_ID",
          "required": false,
          "secret": false,
          "hint": "Twitch app Client ID from https://dev.twitch.tv/console/apps"
        },
        {
          "key": "IGDB_CLIENT_SECRET",
          "required": false,
          "secret": true,
          "hint": "Twitch app Client Secret (New Secret) — powers accurate backlog hours"
        }
      ]
    }
  ]
}
